# Package
version     = "1.5.4"
author      = "Amru Rosyada"
description = "High performance asynchttpserver and web framework for nim lang."
license     = "BSD"
#srcDir      = "src"

# Deps
requires "nim >= 2.0.0"
requires "zfblast >= 0.4.5"
requires "uri3 >= 0.1.7"
requires "stdext >= 0.2.4"
requires "zip >= 0.3.1"
requires "regex >= 0.20.2"
requires "checksums"
