# Package
version     = "1.1.2"
author      = "Amru Rosyada"
description = "High performance asynchttpserver and web framework for nim lang."
license     = "BSD"
srcDir      = "src"

# Deps
requires "nim >= 1.4.0"
requires "zfblast >= 0.2.0"
requires "uri3 >= 0.1.4"
requires "stdext >= 0.0.7"
